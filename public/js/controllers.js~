(function ()
{
    var appControllers = angular.module('appControllers', ["$scope","$http", "ngRoute"]);

    appControllers.controller('PostsListCtrl', ['$scope', '$http',
        function ($scope, $http) {
           //$http.get('posts').success(function (data) {
            //    $scope.posts = data;
             //   console.log(data);
            //});

            //$scope.orderProp = 'age';
        }]);

    appControllers.controller('PostsDetailCtrl', ['$scope', '$routeParams',
        function ($scope, $routeParams) {
            $scope.postId = $routeParams.postId;
        }]);

    appControllers.controller('HelloController', ['$http', '$scope', function ($http, $scope) {

            $scope.posts = $http.get('/posts');
            

        }]);

})();
